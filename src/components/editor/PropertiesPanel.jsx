import { FiPlus, FiTrash2, FiX } from 'react-icons/fi';
export default function PropertiesPanel({ node, onClose, onUpdate, onAddAttribute, onUpdateAttribute, onRemoveAttribute, onDelete }) {
  if (!node) return null;
  const title = node.data.title.replace(/\.java$/, '');
  return <aside className="absolute right-4 top-4 z-20 w-80 rounded-xl border border-slate-700 bg-[#101a31] p-4 text-xs text-slate-200 shadow-2xl"><div className="mb-4 flex items-center justify-between"><b className="text-sm">Propiedades de clase</b><button onClick={onClose} className="rounded p-1 hover:bg-slate-700"><FiX /></button></div><label className="mb-1 block text-slate-400">Nombre de entidad</label><input value={title} onChange={(e) => onUpdate({ title: `${e.target.value}.java` })} className="mb-4 w-full rounded border border-slate-600 bg-[#080f20] px-3 py-2 outline-none focus:border-indigo-400" />
    <div className="mb-2 flex items-center justify-between"><b>Atributos</b><button onClick={onAddAttribute} className="flex items-center gap-1 rounded bg-indigo-600 px-2 py-1 hover:bg-indigo-500"><FiPlus /> Añadir</button></div><div className="max-h-60 space-y-2 overflow-auto">{node.data.properties.map((attr, index) => <div key={index} className="flex gap-1"><input aria-label="Nombre" value={attr.name} onChange={(e) => onUpdateAttribute(index, { name: e.target.value })} className="min-w-0 flex-1 rounded border border-slate-700 bg-[#080f20] px-2 py-1.5" /><input aria-label="Tipo" value={attr.type} onChange={(e) => onUpdateAttribute(index, { type: e.target.value })} className="w-24 rounded border border-slate-700 bg-[#080f20] px-2 py-1.5" /><button onClick={() => onRemoveAttribute(index)} className="rounded px-1 text-rose-300 hover:bg-rose-500/20"><FiTrash2 /></button></div>)}</div>
    <button onClick={onDelete} className="mt-5 flex w-full items-center justify-center gap-2 rounded border border-rose-500/50 px-3 py-2 text-rose-300 hover:bg-rose-500/15"><FiTrash2 /> Eliminar entidad</button></aside>;
}
