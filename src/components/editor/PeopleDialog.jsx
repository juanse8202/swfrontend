import { FiEye, FiUsers, FiX } from 'react-icons/fi';

export default function PeopleDialog({ onClose, members = [] }) {
  return <div className="fixed inset-0 z-40 grid place-items-center bg-[#030715]/80 p-4 backdrop-blur-sm"><section className="w-full max-w-lg rounded-2xl border border-indigo-400/35 bg-[#101a31] p-6 shadow-2xl"><div className="mb-5 flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-600/30 text-indigo-200"><FiUsers /></span><div><h2 className="font-bold text-white">Personas en línea</h2><p className="text-xs text-emerald-300">● {members.length} colaboradores registrados</p></div></div><button onClick={onClose} className="rounded p-2 text-slate-400 hover:bg-slate-700"><FiX /></button></div>{members.map((member) => { const user = member.usuario || {}; const name = user.username || user.email || 'Usuario'; return <div className="mb-3 flex items-center gap-3 rounded-xl border border-slate-700 bg-[#0c162c] p-3" key={member.id}><span className="grid h-10 w-10 place-items-center rounded-full bg-indigo-600 text-xs font-bold text-white">{name.slice(0, 2).toUpperCase()}</span><div className="flex-1"><b className="text-sm text-white">{name}</b><span className="ml-2 text-xs text-slate-400">{member.rol}</span><small className="mt-1 block text-emerald-300">● {user.email}</small></div><button className="rounded border border-slate-600 p-2 text-slate-300 hover:bg-slate-700" title="Seguir"><FiEye /></button></div>; })}{members.length === 0 && <p className="py-6 text-center text-sm text-slate-400">No hay miembros en este proyecto.</p>}</section></div>;
}
