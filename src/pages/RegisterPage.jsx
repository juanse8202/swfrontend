import { useState } from 'react';
import { iniciarOAuth, registrar } from '../api/authApi';

export default function RegisterPage({ onRegisterSuccess, onLogin }) {
  const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [loading, setLoading] = useState(false); const [error, setError] = useState('');
  const submit = async (event) => { event.preventDefault(); setLoading(true); setError(''); try { await registrar(email, password); onRegisterSuccess(); } catch (err) { setError(err.message || 'No se pudo crear la cuenta'); setLoading(false); } };
  return <main className="grid min-h-screen place-items-center bg-surface px-5 text-on-surface"><section className="w-full max-w-md rounded-[28px] border border-outline-variant/30 bg-surface-container-low p-7 shadow-2xl"><div className="mb-7 text-center"><div className="mx-auto mb-3 grid h-11 w-11 place-items-center rounded-xl bg-gradient-to-br from-primary-container to-secondary-container text-xl text-white">◈</div><h1 className="text-xl font-bold">Crear cuenta en DiagramCraft</h1><p className="mt-2 text-sm text-on-surface-variant">Usa tu correo o un proveedor de desarrollo.</p></div><form onSubmit={submit} className="space-y-4"><label className="block text-xs font-semibold text-on-surface-variant">Correo electrónico<input type="email" name="email" value={email} onChange={(e) => setEmail(e.target.value)} required autoComplete="email" placeholder="correo@ejemplo.com" className="mt-2 w-full rounded-lg bg-surface-container-high px-4 py-3 text-sm outline-none ring-primary focus:ring-1" /></label><label className="block text-xs font-semibold text-on-surface-variant">Contraseña<input type="password" name="password" value={password} onChange={(e) => setPassword(e.target.value)} required minLength="8" autoComplete="new-password" placeholder="Mínimo 8 caracteres" className="mt-2 w-full rounded-lg bg-surface-container-high px-4 py-3 text-sm outline-none ring-primary focus:ring-1" /></label>{error && <p className="rounded-lg bg-error-container/30 p-3 text-xs text-error">{error}</p>}<button disabled={loading} className="w-full rounded-lg bg-gradient-to-r from-primary-container to-secondary-container px-4 py-3 font-bold text-white disabled:opacity-60">{loading ? 'Creando cuenta…' : 'Crear cuenta'}</button></form><div className="my-6 flex items-center gap-3 text-[10px] text-outline"><i className="h-px flex-1 bg-outline-variant" />O continúa con<i className="h-px flex-1 bg-outline-variant" /></div><div className="grid grid-cols-3 gap-2">{['github', 'google', 'gitlab'].map((provider) => <button key={provider} onClick={() => iniciarOAuth(provider)} className="rounded-lg border border-outline-variant/40 bg-surface-container px-2 py-3 text-xs capitalize hover:bg-surface-container-high">{provider}</button>)}</div><p className="mt-6 text-center text-xs text-on-surface-variant">¿Ya tienes una cuenta? <button onClick={onLogin} className="font-bold text-primary">Inicia sesión</button></p></section></main>;
}
